model:
  name: dense_world_model
  latent_type: spatial
  latent_channels: 24
  latent_dim: 64
  depth: 3
losses:
  recon_weight: 0.2
  rollout_weight: 1.0
  rollout_gamma: 0.95
  observable_weight: 0.2
  latent_l2_weight: 1.0e-4
